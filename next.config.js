/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/GFE-hero-simple',
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;